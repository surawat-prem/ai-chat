apiVersion: v1
kind: Service
metadata:
  name: chat-server
  namespace: app
spec:
  selector:
    app: chat-server
  type: ClusterIP
  ports:
  - name: chat-server
    protocol: TCP
    port: 80
    targetPort: 80